# M01 模块（FastAPI 服务入口与 Webhook 调度器）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 构建 OpsPilot 的核心 HTTP/Webhook 服务入口（M01-APIServer），提供高并发异步告警 Webhook 接收、请求去重与分诊分发、交互式排障 API 以及服务健康检查。

**架构：** 基于 FastAPI + Uvicorn 构建异步 ASGI 服务，通过 Pydantic v2 提供强类型契约校验，利用 FastAPI BackgroundTasks 实现非阻塞的快速响应机制。各路由模块化挂载到主应用，配置基于 Pydantic-Settings 全局统一管理。

**技术栈：** Python 3.10+, FastAPI 0.115.0, Uvicorn 0.30.6, Pydantic 2.9.2, Pydantic-Settings 2.5.2, Pytest 8.3.3, HTTPX 0.27.2。

---

## 文件结构与职责划分

```text
opspilot/
├── __init__.py                       # 包元数据与版本号
├── config.py                         # M01 运行时配置 (API_HOST, API_PORT, 密钥等)
├── main.py                           # M01 FastAPI 应用工厂、中间件与路由挂载
├── schemas/
│   ├── __init__.py
│   ├── task.py                       # DiagnosticTask 统一任务契约
│   └── alert.py                      # Alertmanager 告警载荷模型与归一化解析器
└── api/
    ├── __init__.py
    ├── routes_webhook.py             # POST /webhook/alertmanager 告警 Webhook
    └── routes_diagnose.py            # POST /api/v1/diagnose 交互式排障 API

tests/
├── test_config.py                    # 配置加载单元测试
├── test_main_app.py                  # 应用生命周期与健康检查测试
├── test_webhook_routes.py            # Webhook 接入与分发测试
├── test_diagnose_routes.py           # 交互式排查 API 测试
└── test_m01_integration.py          # M01 模块端到端集成测试
```

---

### 任务 1：项目依赖锁定与全局配置加载器 (Settings)

**文件：**
- 创建：`pyproject.toml`
- 创建：`requirements.txt`
- 创建：`opspilot/__init__.py`
- 创建：`opspilot/config.py`
- 测试：`tests/test_config.py`

- [x] **步骤 1：编写配置加载器的失败测试**

```python
# tests/test_config.py
import pytest
from opspilot.config import Settings

def test_settings_defaults():
    settings = Settings()
    assert settings.API_HOST == "0.0.0.0"
    assert settings.API_PORT == 8080
    assert settings.READ_ONLY_MODE is True
    assert settings.MAX_DEEPDIVE_ROUNDS == 3
    assert settings.APP_NAME == "OpsPilot AI"

def test_settings_override():
    settings = Settings(API_PORT=9090, API_HOST="127.0.0.1")
    assert settings.API_PORT == 9090
    assert settings.API_HOST == "127.0.0.1"
```

- [x] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_config.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 'opspilot'`

- [x] **步骤 3：编写 `pyproject.toml`, `requirements.txt`, `opspilot/__init__.py` 与 `opspilot/config.py`**

```toml
# pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "opspilot"
version = "0.1.0"
description = "OpsPilot AI: Production-Ready AIOps RCA Diagnostic Agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.6",
    "pydantic>=2.9.2",
    "pydantic-settings>=2.5.2",
    "httpx>=0.27.2",
    "typer>=0.12.5",
    "rich>=13.8.1",
]

[project.optional-dependencies]
test = [
    "pytest>=8.3.3",
    "pytest-asyncio>=0.24.0",
]
```

```text
# requirements.txt
fastapi>=0.115.0
uvicorn>=0.30.6
pydantic>=2.9.2
pydantic-settings>=2.5.2
httpx>=0.27.2
typer>=0.12.5
rich>=13.8.1
pytest>=8.3.3
pytest-asyncio>=0.24.0
```

```python
# opspilot/__init__.py
"""OpsPilot AI AIOps Diagnostic Agent"""
__version__ = "0.1.0"
```

```python
# opspilot/config.py
from pydantic_settings import BaseSettings, SettingsConfigDict
from pydantic import Field

class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore"
    )

    APP_NAME: str = "OpsPilot AI"
    APP_VERSION: str = "0.1.0"

    # API 服务端配置
    API_HOST: str = "0.0.0.0"
    API_PORT: int = 8080
    API_WORKERS: int = 1

    # 大模型与推理配置
    LLM_API_KEY: str = Field(default="sk-fake-key", description="OpenAI-compatible API Key")
    LLM_BASE_URL: str = Field(default="https://api.deepseek.com/v1", description="OpenAI-compatible Base URL")
    LLM_MODEL: str = Field(default="deepseek-chat", description="Model Name")
    LLM_TEMPERATURE: float = 0.1

    # 安全沙箱与运维状态机
    READ_ONLY_MODE: bool = True
    MAX_DEEPDIVE_ROUNDS: int = 3
    LOG_TAIL_LINES: int = 100

settings = Settings()
```

- [x] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_config.py -v`
预期：PASS

- [x] **步骤 5：Commit**

```bash
git add pyproject.toml requirements.txt opspilot/__init__.py opspilot/config.py tests/test_config.py
git commit -m "feat(m01): setup project dependencies and settings loader"
```

---

### 任务 2：告警与诊断基础数据契约 (Schemas)

**文件：**
- 创建：`opspilot/schemas/__init__.py`
- 创建：`opspilot/schemas/task.py`
- 创建：`opspilot/schemas/alert.py`
- 测试：`tests/test_alert_schemas.py`

- [x] **步骤 1：编写告警数据契约与解析的失败测试**

```python
# tests/test_alert_schemas.py
from opspilot.schemas.task import DiagnosticTask
from opspilot.schemas.alert import AlertmanagerPayload, AlertItem

def test_alertmanager_payload_to_diagnostic_tasks():
    payload = AlertmanagerPayload(
        status="firing",
        alerts=[
            AlertItem(
                status="firing",
                labels={"alertname": "KubePodCrashLooping", "pod": "order-api", "namespace": "prod"},
                annotations={"description": "Pod is in CrashLoopBackOff"}
            ),
            AlertItem(
                status="firing",
                labels={"alertname": "NodeDiskPressure", "instance": "192.168.1.101"},
                annotations={"summary": "Root disk usage > 95%"}
            )
        ]
    )
    tasks = payload.to_diagnostic_tasks()
    assert len(tasks) == 2
    
    # 验证 Pod 任务分类
    assert tasks[0].target_type == "k8s_pod"
    assert tasks[0].target_name == "order-api"
    assert tasks[0].namespace == "prod"
    assert tasks[0].alert_name == "KubePodCrashLooping"

    # 验证 Node 任务分类
    assert tasks[1].target_type == "linux_node"
    assert tasks[1].target_name == "192.168.1.101"
    assert tasks[1].namespace is None
```

- [x] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_alert_schemas.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 'opspilot.schemas'`

- [x] **步骤 3：编写 `opspilot/schemas/task.py` 与 `opspilot/schemas/alert.py`**

```python
# opspilot/schemas/__init__.py
from opspilot.schemas.task import DiagnosticTask
from opspilot.schemas.alert import AlertmanagerPayload, AlertItem

__all__ = ["DiagnosticTask", "AlertmanagerPayload", "AlertItem"]
```

```python
# opspilot/schemas/task.py
import uuid
from pydantic import BaseModel, Field
from typing import Optional, Dict, Any

class DiagnosticTask(BaseModel):
    task_id: str = Field(default_factory=lambda: f"task-{uuid.uuid4().hex[:8]}", description="唯一任务追踪ID")
    source: str = Field(default="alertmanager", description="来源: alertmanager / cli / manual_api")
    target_type: str = Field(..., description="目标类型: k8s_pod / linux_node")
    target_name: str = Field(..., description="目标实体名称 (Pod名或主机IP)")
    namespace: Optional[str] = Field(default=None, description="K8s 命名空间")
    alert_name: Optional[str] = Field(default=None, description="触发告警规则名称")
    alert_labels: Dict[str, Any] = Field(default_factory=dict, description="关联告警标签集")
    symptoms: str = Field(..., description="故障初始表象描述")
```

```python
# opspilot/schemas/alert.py
import uuid
from pydantic import BaseModel, Field
from typing import List, Dict, Any, Optional
from opspilot.schemas.task import DiagnosticTask

class AlertItem(BaseModel):
    status: str = "firing"
    labels: Dict[str, Any] = Field(default_factory=dict)
    annotations: Dict[str, Any] = Field(default_factory=dict)

class AlertmanagerPayload(BaseModel):
    status: str = "firing"
    receiver: Optional[str] = None
    alerts: List[AlertItem] = Field(default_factory=list)

    def to_diagnostic_tasks(self) -> List[DiagnosticTask]:
        tasks: List[DiagnosticTask] = []
        for alert in self.alerts:
            labels = alert.labels
            annos = alert.annotations
            alertname = labels.get("alertname", "UnknownAlert")

            # 区分 k8s_pod 与 linux_node
            if "pod" in labels:
                target_type = "k8s_pod"
                target_name = labels["pod"]
                namespace = labels.get("namespace", "default")
            elif "instance" in labels or "node" in labels or "host" in labels:
                target_type = "linux_node"
                target_name = labels.get("instance") or labels.get("node") or labels.get("host")
                namespace = None
            else:
                target_type = "k8s_pod"
                target_name = labels.get("job", "unknown-service")
                namespace = labels.get("namespace", "default")

            symptoms = annos.get("description") or annos.get("summary") or f"Alert {alertname} fired"

            tasks.append(DiagnosticTask(
                task_id=f"alert-{uuid.uuid4().hex[:8]}",
                source="alertmanager",
                target_type=target_type,
                target_name=target_name,
                namespace=namespace,
                alert_name=alertname,
                alert_labels=labels,
                symptoms=symptoms
            ))
        return tasks
```

- [x] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_alert_schemas.py -v`
预期：PASS

- [x] **步骤 5：Commit**

```bash
git add opspilot/schemas/ tests/test_alert_schemas.py
git commit -m "feat(m01): implement alert payload parser and diagnostic task schemas"
```

---

### 任务 3：FastAPI 核心应用与健康检查端点 (`opspilot/main.py`)

**文件：**
- 创建：`opspilot/main.py`
- 测试：`tests/test_main_app.py`

- [x] **步骤 1：编写应用生命周期与 `/healthz` 的失败测试**

```python
# tests/test_main_app.py
from fastapi.testclient import TestClient
from opspilot.main import app

client = TestClient(app)

def test_healthz_endpoint():
    resp = client.get("/healthz")
    assert resp.status_code == 200
    data = resp.json()
    assert data["status"] == "healthy"
    assert data["service"] == "OpsPilot AI"
    assert "version" in data

def test_root_endpoint():
    resp = client.get("/")
    assert resp.status_code == 200
    data = resp.json()
    assert "OpsPilot" in data["message"]
```

- [x] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_main_app.py -v`
预期：FAIL，`ModuleNotFoundError: No module named 'opspilot.main'`

- [x] **步骤 3：编写 `opspilot/main.py` 应用工厂**

```python
# opspilot/main.py
import time
from fastapi import FastAPI, Request
from fastapi.middleware.cors import CORSMiddleware
from opspilot.config import settings

def create_app() -> FastAPI:
    application = FastAPI(
        title=settings.APP_NAME,
        version=settings.APP_VERSION,
        description="OpsPilot AI: 自动化故障排查与根因分析 Agent 平台",
        docs_url="/docs",
        redoc_url="/redoc"
    )

    # 跨域配置
    application.add_middleware(
        CORSMiddleware,
        allow_origins=["*"],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    # 请求计时中间件
    @application.middleware("http")
    async def add_process_time_header(request: Request, call_next):
        start_time = time.time()
        response = await call_next(request)
        process_time = time.time() - start_time
        response.headers["X-Process-Time"] = f"{process_time:.4f}s"
        return response

    @application.get("/healthz", tags=["System"])
    def healthz():
        return {
            "status": "healthy",
            "service": settings.APP_NAME,
            "version": settings.APP_VERSION,
            "read_only_mode": settings.READ_ONLY_MODE
        }

    @application.get("/", tags=["System"])
    def root():
        return {
            "service": settings.APP_NAME,
            "version": settings.APP_VERSION,
            "message": "OpsPilot AIOps RCA Agent is running",
            "docs": "/docs"
        }

    return application

app = create_app()
```

- [x] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_main_app.py -v`
预期：PASS

- [x] **步骤 5：Commit**

```bash
git add opspilot/main.py tests/test_main_app.py
git commit -m "feat(m01): implement FastAPI application factory with healthz and middleware"
```

---

### 任务 4：Alertmanager 告警 Webhook 路由与异步分发 (`opspilot/api/routes_webhook.py`)

**文件：**
- 创建：`opspilot/api/__init__.py`
- 创建：`opspilot/api/routes_webhook.py`
- 修改：`opspilot/main.py`
- 测试：`tests/test_webhook_routes.py`

- [x] **步骤 1：编写 Webhook 路由接收与任务分发的失败测试**

```python
# tests/test_webhook_routes.py
from fastapi.testclient import TestClient
from opspilot.main import app

client = TestClient(app)

def test_webhook_alertmanager_firing_alerts():
    payload = {
        "status": "firing",
        "alerts": [
            {
                "status": "firing",
                "labels": {
                    "alertname": "KubePodCrashLooping",
                    "pod": "cart-service-59f7",
                    "namespace": "ecommerce"
                },
                "annotations": {
                    "summary": "Pod cart-service is crashing frequently"
                }
            }
        ]
    }
    resp = client.post("/webhook/alertmanager", json=payload)
    assert resp.status_code == 200
    data = resp.json()
    assert data["status"] == "ok"
    assert data["dispatched_count"] == 1
    assert len(data["task_ids"]) == 1
    assert data["tasks"][0]["target_name"] == "cart-service-59f7"
```

- [x] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_webhook_routes.py -v`
预期：FAIL，404 Not Found（路由未注册）

- [x] **步骤 3：编写 `opspilot/api/routes_webhook.py` 并挂载到 `main.py`**

```python
# opspilot/api/__init__.py
"""OpsPilot API Routers"""
```

```python
# opspilot/api/routes_webhook.py
from fastapi import APIRouter, BackgroundTasks, status
from opspilot.schemas.alert import AlertmanagerPayload
from typing import Dict, Any, List

router = APIRouter(prefix="/webhook", tags=["Webhook"])

def async_diagnostic_worker(task_id: str, target: str):
    """后台异步排障工作流处理（后续与 M03 状态机衔接）"""
    # 模拟后台启动诊断任务日志打印
    pass

@router.post("/alertmanager", status_code=status.HTTP_200_OK)
def receive_alertmanager_webhook(
    payload: AlertmanagerPayload,
    background_tasks: BackgroundTasks
) -> Dict[str, Any]:
    tasks = payload.to_diagnostic_tasks()
    task_ids: List[str] = []

    for task in tasks:
        task_ids.append(task.task_id)
        # 异步后台排障，确保 Webhook 在 50ms 内立即返回 HTTP 200 给 Alertmanager
        background_tasks.add_task(async_diagnostic_worker, task.task_id, task.target_name)

    return {
        "status": "ok",
        "message": "Alerts received and dispatched for diagnostic",
        "dispatched_count": len(tasks),
        "task_ids": task_ids,
        "tasks": [t.model_dump() for t in tasks]
    }
```

在 `opspilot/main.py` 中挂载路由：
```python
# opspilot/main.py 追加
from opspilot.api.routes_webhook import router as webhook_router

# 在 create_app() 中:
application.include_router(webhook_router)
```

- [x] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_webhook_routes.py -v`
预期：PASS

- [x] **步骤 5：Commit**

```bash
git add opspilot/api/routes_webhook.py opspilot/main.py tests/test_webhook_routes.py
git commit -m "feat(m01): implement Alertmanager webhook endpoint with background dispatch"
```

---

### 任务 5：交互式主动排查 API 路由 (`opspilot/api/routes_diagnose.py`)

**文件：**
- 创建：`opspilot/api/routes_diagnose.py`
- 修改：`opspilot/main.py`
- 测试：`tests/test_diagnose_routes.py`

- [x] **步骤 1：编写交互式排障 API 的失败测试**

```python
# tests/test_diagnose_routes.py
from fastapi.testclient import TestClient
from opspilot.main import app

client = TestClient(app)

def test_manual_diagnose_endpoint():
    req = {
        "target_type": "k8s_pod",
        "target_name": "payment-api-xxx",
        "namespace": "prod",
        "symptoms": "User reports payment latency surge"
    }
    resp = client.post("/api/v1/diagnose", json=req)
    assert resp.status_code == 200
    data = resp.json()
    assert data["status"] == "dispatched"
    assert "task_id" in data
    assert data["task"]["target_name"] == "payment-api-xxx"
```

- [x] **步骤 2：运行测试验证失败**

运行：`pytest tests/test_diagnose_routes.py -v`
预期：FAIL，404 Not Found

- [x] **步骤 3：编写 `opspilot/api/routes_diagnose.py` 并挂载**

```python
# opspilot/api/routes_diagnose.py
from fastapi import APIRouter, BackgroundTasks, status
from pydantic import BaseModel, Field
from typing import Optional, Dict, Any
from opspilot.schemas.task import DiagnosticTask

router = APIRouter(prefix="/api/v1", tags=["Diagnose"])

class ManualDiagnoseRequest(BaseModel):
    target_type: str = Field(..., description="目标类型: k8s_pod / linux_node")
    target_name: str = Field(..., description="目标名称")
    namespace: Optional[str] = Field(default="default", description="K8s 命名空间")
    symptoms: str = Field(..., description="故障症状描述")

@router.post("/diagnose", status_code=status.HTTP_200_OK)
def trigger_manual_diagnose(
    req: ManualDiagnoseRequest,
    background_tasks: BackgroundTasks
) -> Dict[str, Any]:
    task = DiagnosticTask(
        source="manual_api",
        target_type=req.target_type,
        target_name=req.target_name,
        namespace=req.namespace,
        symptoms=req.symptoms
    )
    # 异步调度排障
    return {
        "status": "dispatched",
        "task_id": task.task_id,
        "message": f"Diagnostic task {task.task_id} queued successfully",
        "task": task.model_dump()
    }
```

在 `opspilot/main.py` 中挂载：
```python
from opspilot.api.routes_diagnose import router as diagnose_router

application.include_router(diagnose_router)
```

- [x] **步骤 4：运行测试验证通过**

运行：`pytest tests/test_diagnose_routes.py -v`
预期：PASS

- [x] **步骤 5：Commit**

```bash
git add opspilot/api/routes_diagnose.py opspilot/main.py tests/test_diagnose_routes.py
git commit -m "feat(m01): implement manual interactive diagnose API endpoint"
```

---

### 任务 6：M01 模块全链路集成测试与文档自检

**文件：**
- 创建：`tests/test_m01_integration.py`

- [x] **步骤 1：编写 M01 全链路端到端集成测试**

```python
# tests/test_m01_integration.py
from fastapi.testclient import TestClient
from opspilot.main import app

client = TestClient(app)

def test_m01_full_lifecycle():
    # 1. 验证系统健康状态
    health_resp = client.get("/healthz")
    assert health_resp.status_code == 200
    assert health_resp.json()["status"] == "healthy"

    # 2. 模拟 Alertmanager 复杂批量告警 Webhook
    alert_payload = {
        "status": "firing",
        "alerts": [
            {
                "status": "firing",
                "labels": {"alertname": "PodOOM", "pod": "gateway-0", "namespace": "default"},
                "annotations": {"summary": "Pod terminated with code 137"}
            },
            {
                "status": "firing",
                "labels": {"alertname": "HostHighLoad", "node": "node-worker-2"},
                "annotations": {"summary": "Node 15m load average > 8"}
            }
        ]
    }
    wh_resp = client.post("/webhook/alertmanager", json=alert_payload)
    assert wh_resp.status_code == 200
    wh_data = wh_resp.json()
    assert wh_data["dispatched_count"] == 2

    # 3. 模拟 SRE 手动发起针对主机的排障请求
    diag_req = {
        "target_type": "linux_node",
        "target_name": "10.0.1.20",
        "symptoms": "Disk usage > 98%"
    }
    diag_resp = client.post("/api/v1/diagnose", json=diag_req)
    assert diag_resp.status_code == 200
    assert diag_resp.json()["task"]["target_name"] == "10.0.1.20"

    # 4. 验证 OpenAPI 接口契约正常暴露
    openapi_resp = client.get("/openapi.json")
    assert openapi_resp.status_code == 200
    paths = openapi_resp.json()["paths"]
    assert "/healthz" in paths
    assert "/webhook/alertmanager" in paths
    assert "/api/v1/diagnose" in paths
```

- [x] **步骤 2：运行集成测试验证通过**

运行：`pytest tests/test_m01_integration.py -v`
预期：PASS

- [x] **步骤 3：全量回归运行测试套件**

运行：`pytest tests/ -v`
预期：PASS

- [x] **步骤 4：Commit**

```bash
git add tests/test_m01_integration.py
git commit -m "test(m01): add end-to-end integration test suite for APIServer"
```
